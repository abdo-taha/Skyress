{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "skyress.serviceAccountName" . }}
  labels:
    {{- include "skyress.labels" . | nindent 4 }}
{{- end }}
