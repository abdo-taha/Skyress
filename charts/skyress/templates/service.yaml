apiVersion: v1
kind: Service
metadata:
  name: {{ include "skyress.fullname" . }}
  labels:
    {{- include "skyress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skyress.selectorLabels" . | nindent 4 }}
